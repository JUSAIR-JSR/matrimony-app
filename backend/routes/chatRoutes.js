const express = require('express');
const router = express.Router();

// Placeholder for chat routes
router.get('/', (req, res) => {
  res.send('Chat routes will be implemented here');
});

module.exports = router;